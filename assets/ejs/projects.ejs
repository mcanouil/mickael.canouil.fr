:::: {.grid .text-center .list}

<% for (const item of items) { %>

:::: {.g-col-12 .g-col-lg-6 .g-col-xl-4 <%= metadataAttrs(item) %>}

:::: {.quarto-grid-item .card .h-100 .card-center .projects}

```{=html}
<div class="card-img-top">
<a href="<%- item.path %>">
  <img src="<%= item.image %>" class="thumbnail-image card-img" loading="lazy" alt="<%= item.title %>">
</a>
</div>
```

:::: {.card-body .post-contents}

[{{< iconify octicon:mark-github-16 title="GitHub Repository" label="GitHub Repository" >}}](<%- item.path %>){#github-link .quarto-grid-link style="position: absolute; top: 0; right: 0; padding: 0.5em;"}

```{=html}
<a class="quarto-grid-link" href="<%- item.path %>">
  <h5 class="no-anchor card-title listing-title"><%= item.title %></h5>
</a>
```

:::: {.listing-categories}
```{=html}
<% for (const category of item.categories) { %>
<div class="listing-category" onclick="window.quartoListingCategory('<%= utils.b64encode(category) %>'); return false;"><%= category %></div>
<% } %>
```
:::

:::: {.card-text}
:::: {.listing-description}
<%= item.description %>
:::

:::: {.listing-modal}
<%
const usage = item.usage;
const id = utils.b64encode(usage);
if (usage) {
%>
```{=html}
<button type="button" class="btn btn-outline-light" style="text-align: center;" data-bs-toggle="modal" data-bs-target="#installModal-<%= id %>">Install</button>
<%
if (item.template === true) {
%>
<button type="button" class="btn btn-outline-light" style="text-align: center;" data-bs-toggle="modal" data-bs-target="#useModal-<%= id %>">Use</button>
<%
}
%>
```
<%
}
%>
:::

:::

:::

:::: {.card-footer}
:::: {.listing-date}
<%= item.date %>
:::
:::

:::

:::

<% } %>

:::

```{=html}
<!-- Modals placed at top level to avoid positioning issues -->
<% for (const item of items) { %>
<%
const usage = item.usage;
if (usage) {
const id = utils.b64encode(usage);
partial("extension-install-modal.ejs", { id, usage });
if (item.template === true) {
partial("extension-use-modal.ejs", { id, usage });
}
}
%>
<% } %>
```
